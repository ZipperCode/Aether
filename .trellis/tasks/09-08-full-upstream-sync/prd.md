# 完整同步上游 main 并保留本地增强

## Goal

完整合并 `fawney19/Aether/main` 到本地 `master`，保留 fork 已有增强，交付可验证的整合代码。

## Confirmed Scope

- 用户先要求“同步上游更新”，随后明确回答“确定，继续”，批准完整合并，包括此前排除的 VSCodex 等功能、只保留 PostgreSQL、原始 HTTP Header 持久化、容器 root 运行配置。
- 起点：`b0ad8ff7f7bdb1888abac8c06f47f61e76761431`；上游目标：`c7e403b410139d12a6189dda9c2bdf0c7c80782e`。
- 起点工作区干净，比 `origin/master=876a4fb5a95a95d9ccd6493b818176ae53e87eeb` 领先 4 个提交；这些提交及既有历史必须保留。
- 在隔离 worktree 整合和检查，完成后合回本地 `master` 并清理任务分支与 worktree。

## Requirements

1. 使用真实 Git merge 保留双方历史，接入锁定上游的完整功能，不再按旧任务的功能排除清单裁剪。
2. 保留本地认证配置 revision 快照、轻量认证/模型发现投影、候选惰性 transport hydration、批次级白名单 reconcile、受限维护并发和单次启动 Pool rebuild。
3. 保留本地 Endpoint 精确证据关联、余额调度、运行时额度阻断与人工恢复、模型能力测试及既有 Responses HTTP/WS 行为；仅用上游明确的新目标语义替换旧实现，例如每候选首字节超时。
4. PostgreSQL-only 收敛包括删除失效的 MySQL/SQLite 调用、测试和契约表述；保留适用的 memory/PostgreSQL 本地增强，不新增数据库迁移兼容路线。
5. 保留 fork 仓库身份、既有发布版本及私有部署命名；同步上游工作流功能时使用本仓库命名，不把默认镜像误改为源仓库制品。
6. 对本次手工解决冲突的新改函数、字段补齐有意义的中文说明；直接合入的上游内容不做无关全仓注释或重构。

## Acceptance Criteria

- [ ] 合并提交同时保有起点和上游目标为祖先，无未解决冲突及冲突标记。
- [x] 上游主要功能均有实际代码入口，重要行为替换、冲突处理和本地契约保留有记录。
- [x] 本地内存、Endpoint、余额/额度阻断及 Responses 关键增强未被整段覆盖或静默删除。
- [x] 完成解析/格式、相关类型检查、合并影响到的编译与最小逻辑回归；失败按证据修复，环境阻塞与未验证项如实记录。
- [ ] 本地 master 接入最终已检查结果，隔离工作区和临时分支安全清理，现有 .env、运行服务和数据库不受影响。

## Out of Scope

- push、tag、GitHub Release、远程部署、修改或迁移现有数据库、启动现有 Compose 环境。
- 删除或回滚不属于本任务的文件、提交或进程；可使用完全任务自有的临时编译与测试资源。
- UI 浏览器/截图/视觉验证；仅对相关逻辑和类型进行必要检查。
