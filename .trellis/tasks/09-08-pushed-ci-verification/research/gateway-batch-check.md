# Gateway batch integration check

- Status: READY under Root's explicitly revised local gate; reviewer has stopped product writes, 2026-09-08. This is not a final-SHA Linux CI PASS.
- Source baseline: `948c1c16f2f9927b37a8570b86767128abd6b7c8` plus the 27 task-owned tracked changes. All leaf writers have stopped; no commit/push or external operation is authorized for this reviewer.
- Loaded full saved hook, PRD/design/implement/check context, immutable CI run inventory, four Gateway receipts, first-repair receipt, package instructions/specs, and `trellis-check`/`ponytail` guidance.

## Review

- The only second-wave production change is the shared admin quota projection. Actual `ProviderQuotaBalance` has a unit and five optional decimal strings; frontend consumers accept these exact keys. Gateway materialization recognizes balances and the final response projects again, so fixing the shared projector covers all callers. Amount parsing only checks validity; the original string is cloned, including precision/trailing zeroes. Existing unknown diagnostic omission is retained.
- A: current source explicitly excludes routed resolved pages, unconditional known subscription exhaustion matches the current spec, the cold-key snapshot now models a complete catalog, and architecture assertions retain exact ownership/one exact migration-test exception.
- B: current capture engine proves Basic disables bodies and Full leaves capture enabled; tests explicitly select their original intent. Internal miss headers remain client-invisible. Strong Key admission and single data-state replacement explain the catalog/tunnel fixture repairs. Accounting, visible-output, raw-header, full-response and terminal checks remain.
- C: ordinary config reads/listing omit null tombstones while strong revision reads retain them. Restricted Provider IDs require real directory evidence. Exact discovery/Endpoint and provider-template boundaries are preserved.
- D: real Kiro refresh discovers missing Profile after IDC refresh; social refresh accepts `profileArn`. The fixture now models those existing native requests through the local execution override and a real memory node, without changing production auth/transport.
- Static review established no additional defect, but the completed 35-target runtime pass exposed the shared diagnostic registration omission described below.

## Findings fixed by reviewer

- **Production diagnostic registration omission**: `crates/aether-data/contracts/src/repository/candidates/types.rs` did not register `endpoint_capability_mismatch` in `REQUEST_CANDIDATE_ERROR_TYPES`. Both existing real execution paths set this exact internal type (`stream/execution.rs:6155`, `sync/execution.rs:3146`) before calling `record_local_request_candidate_status` (`request_candidate_runtime.rs:362`) and `persist_local_request_candidate_status_record` (`request_candidate_runtime.rs:296`). `GatewayDataState::upsert_request_candidate` (`data/state/catalog.rs:138`) calls `sanitize_for_persistence`, which calls `sanitize_request_candidate_error_type` (`candidates/types.rs:808`); the unregistered code became `unclassified_error`. Memory storage also invokes the same sanitizer. This was not a legitimate reason to weaken the original two Gateway assertions.
- Root granted this single additional product path. Added only the exact existing internal category, plus preservation/repeated-projection checks in the existing `candidate_persistence_keeps_only_known_diagnostic_categories` test. Its unknown-category assertions remain unchanged. No wildcard, new category family, raw diagnostic passthrough, production callback, or Gateway assertion changed.
- PostgreSQL `postgres_candidate_upsert_sql` (`adapters/postgres/src/candidates.rs:427`) imports and consumes the same `REQUEST_CANDIDATE_ERROR_TYPES` at line 443. All three existing UPSERT templates use that function; no handwritten SQL or second registry change is needed.

## Verification so far

- PASS: scoped rustfmt check on all 21 changed Rust files; complete `git diff --check`.
- PASS: `python tests/ci_contract_test.py`.
- PASS: complete workflow YAML parse and equality with baseline after only the two intended Data/Rest `--no-fail-fast` command additions; 17 definitions and two `check_data_features` matrix children give 18 expanded jobs.
- PASS: immutable CI inventory mapped to exactly 35 unique current test names, applying only the two documented renames.
- PASS: `cargo test -p aether-admin --lib provider_status_projection --locked`: 2 passed, 0 failed, 300 filtered; build 6m16s, execution 0.00s; session `20639` exited 0. Native MSVC/Rust 1.95.0, dev/test debug 0, incremental 0, jobs 2, 16 MiB stack. Current artifact: `target/debug/deps/aether_admin-a3e6363145052ebb.exe`.
- PASS: `cargo test -p aether-gateway --lib --no-run --locked` with identical profile/toolchain environment; session `8442` exited 0, build **18m05s**. This was the single Gateway lib test build, not a stale September 5 artifact or a full suite execution.
- Gateway artifact: `target/debug/deps/aether_gateway-632f2856f293c136.exe`, 236,524,032 bytes, written `2026-09-08 10:42:42 UTC`, SHA-256 `7F60E2EB946F1267EF0037E4B9AE0ABB94D6CF02A288E2E684E51FB6E6086E98`. It represents baseline HEAD plus the 27 task changes **before** the reviewer registration fix, not the final post-fix source.
- Completed all 35 exact Gateway targets in separate invocations of that one binary, `--exact --test-threads=1`, `RUST_MIN_STACK=16777216`. Its `--list` contained all 35 names; every invocation required `running 1 test` and a nonzero real test count. Session `24297` returned failure only after collecting the whole batch: **33 passed / 2 failed, 64.135s wall**.
- The only failures were stream `no_local_stream_plans_retries_at_credential_scope_without_key_penalty` at line 11138 and sync `no_local_sync_plans_retries_at_credential_scope_without_key_penalty` at line 4228: stored error type `unclassified_error` versus required `endpoint_capability_mismatch`. Retry scope, preserved error body/internal-header omission and the stream quarantine checks before those assertions passed. Both tunnel tests, balance materialization, all A/C/D cases, accounting release, capture and generated response identity passed.
- Root explicitly revised the final local gate after that evidence: for this single exact-category registration, run the contracts target and check shared SQL registry references; **do not rebuild the entire Gateway again**. The two Gateway targets are therefore not claimed locally PASS after the fix; full final Linux Gateway/CI verification belongs to Root's next exact-SHA push.
- PASS: `cargo test -p aether-data-contracts --lib candidate_persistence_keeps_only_known_diagnostic_categories --locked`, same profile/toolchain, session `9500` exited 0. **1 passed / 0 failed / 224 filtered**, build 57.96s, execution 0.00s. Artifact `target/debug/deps/aether_data_contracts-da59d9b603f274b6.exe` is after the registration fix.
- PASS: `cargo clippy -p aether-admin -p aether-data-contracts --lib --no-deps --locked -- -D warnings`, same environment, session `74078` exited 0 in **2m17s**, no warnings. This lint/type-checks the two final production-owning libraries and their dependencies, not Gateway/whole workspace/tests/features. Final contracts regression artifact SHA-256: `3358E57E2259E8E2671B5B81FA5EF6B4535C750A6B286CB672FC69D60B5C33E8`.
- PASS after registration fix: scoped rustfmt and `git diff --check` on the one additionally granted contracts file. Earlier unchanged format/workflow/frontend results were not rerun.

## Limits and cleanup

- Prior first-batch runtime/TypeScript/lint evidence is reused only as documented in `check-first-repairs.md`; those inputs remain unchanged. Local Vitest 4.0.10 differs from CI lock 4.1.11.
- No full local Gateway/Workspace/Data/frontend suite or real database/provider call. Original Data/Rest 2,222 unrun cases remain for the next exact-SHA Linux no-fail-fast CI; current native checks cannot certify that run.
- Root owns spec follow-up, task state, commit/push and exact-SHA remote verification. No new production file/helper/framework, dependency, configuration, permission, tag/release or deployment change.
- Final product write set is 28 tracked paths: the original 27 plus the one Root-granted diagnostic contracts file. Reviewer changed only that contracts file and this receipt. No code changed during/after final scoped Clippy and contracts regression.
- Root-owned spec follow-up: preserve defined balance fields and exact decimal strings at the shared admin projector; keep repeated projection stable. Trusted internal candidate categories must be registered in the shared contracts list consumed by memory and PostgreSQL; unknown diagnostics must retain existing fallback behavior.
- All Cargo/test sessions exited. No persistent test server/background process or standalone execution helper remains. The two task-only build/test logs under `target/` were removed after preserving counts, errors, timings and artifact hashes here; normal Cargo cache/test binaries were retained. No user files or other owners' changes were removed.
