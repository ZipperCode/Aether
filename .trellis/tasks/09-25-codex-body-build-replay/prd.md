# 回放并修复 Codex 上游请求体构建失败

## 目标

针对用户在 v0.7.39 发布后再次报告的“无法构建上游请求体”失败，从真实 breakpoint/failure_diagnostic/error_message/upstream_response 回放定位，修复实际失效的共享请求构造路径并增加覆盖真实输入的回归。

## 已知事实

- 本地与 origin/master 一致：8e765877e，工作区原本干净；前次修复发布 v0.7.39 源码22124fe59。
- 154 已部署 v0.7.39；13:06 请求 `5a618313-0cff-4e2f-be6c-450639994d48` 的已采集正文确认 `gpt-image-2.5-flare / n=1 / size=1536x1024 / output_format=png`，Codex 被第二层字段名单跳过，custom 提供商成功。
- 用户 13:08:53 禁用 custom 后，13:09 两次 `/v1/images/edits` 只有 Codex skipped 候选，没有 usage/audit 行；容器日志错误地呈现预提交的 HTTP 200，实际没有上游请求。
- breakpoint=$ 为根级诊断，不足以说明是用户JSON错误或上游拒绝；必须查具体上下文与是否真正发出上游请求。

## 验收

- 找到对应失败记录，提供准确模型/接口/版本/错误阶段和具体字段证据。
- 使用无秘密的最小复现覆盖相同失败输入，证明修复前失败与修复后行为。
- 检查所有相关调用方；正式调用与管理模型测试请求构造/诊断保持一致。
- 相关回归、类型/静态检查和规格更新完成；不将本机验证宣称为真实账号成功。
- 全候选跳过的图片同步 JSON 心跳路径必须记录失败 usage 和原有诊断，遵守现有正文采集设置；不能伪造上游响应或已发生的请求。
- 失败记录必须保留已知请求模型；当原有诊断和候选信息均没有模型时，复用原始 JSON 请求中的非空字符串 model，不能将明确的请求型号记为 unknown。
- 已确认的 `output_format` 采用现有公共图片校验并保留到 Codex 上游正文，生成/编辑、管理测试及桥接调用共享修复。
- 候选诊断落库保留既有安全内部 kind/source；不放宽普通用户的信息隔离。

## 边界

生产只读，不更新服务、配置、数据库或Redis，不自动触发真实付费上游请求；不重试掩盖根因，不静默删除用户语义字段。

此次有证据的修复不顺带扩大质量档位、尺寸、mask、流式输出等其他协议行为；13:09 编辑正文未留存，不声称精确还原其字段。

## 完成记录

- 真实13:06参数已构成脱敏回归，确认 Codex 二次字段投影拒绝 output_format；共享生成/编辑构造修复完成。
- 全候选跳过已复现旧 HTTP200，并修复为503与单条 failed/void usage；模型名、采集开关及候选原始诊断保留。
- 120项不同的相关测试、共享库和 Gateway Clippy、全仓格式及字段矩阵检查通过。独立代码审查无问题，详见 verification.md。
- 生产只读核查结束；本次未部署或发起真实付费生成。后续按原授权进入v0.7.40发布。
