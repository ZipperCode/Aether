# Pipeline implementation receipt

## Scope and implementation

- Date: 2026-09-08; local baseline `798ba9d5c8166febab1ab3c56b29f93f17d0e6ee`.
- Sole changed paths: `.github/workflows/rust-ci.yml`, `tools/ci.py`, `tests/ci_contract_test.py`, `Makefile`, `apps/aether-gateway/AGENTS.md` (commands only), this receipt.
- `tools/ci.py` is a stdlib-only dispatcher for `fmt`, `clippy-gateway`, `gateway`, and ordered `preflight`; `--dry-run` prints the actual command arrays and owned nonsecret environment overrides without spawning a command.
- Exact Gateway command: `cargo nextest run -p aether-gateway --lib --bins --no-fail-fast --locked`. Existing lib/bins coverage is retained; sibling failures are collected once and the nonzero status is propagated without retry.
- Format remains `cargo fmt --all --check`; Gateway Clippy remains `cargo clippy -p aether-gateway --lib --bins --examples -- -D warnings`.
- The dispatcher overrides only child incremental/dev debug/test debug settings to `0`, and Gateway thread stack to `16777216` bytes. It inherits host linker/sccache configuration and does not mutate the parent environment, install tools, read `.env`, or start services.
- Existing workflow global profile values remain available during cache restore and for unchanged jobs; the fixture verifies their equality with the dispatcher's owned settings.
- Gateway mold `RUSTFLAGS` is now job-scoped before cache restoration. Its one test step retains `RUSTC_WRAPPER=sccache` and `SCCACHE_GHA_ENABLED=true`; action pins, shared cache key/policy and existing setup actions are untouched.
- All 11 Rust setup steps explicitly select existing repository version `1.95.0`; seven previously omitted setup version. This avoids unused moving stable installation, not proof historical Cargo used a wrong compiler.
- Both push and PR filters now include `rust-toolchain.toml`, `aether-vscodex/**`, `Makefile`, shared entry and both quick regressions. The existing Format job invokes both regressions, without new jobs or dependency edges.
- Thin `make ci` / `make preflight` / `make ci-gateway` aliases and native Python commands are documented, including prerequisites and the Windows/Linux/database parity limitation.

## Retained gates

| Aggregate | Required-success branches (unchanged) |
| --- | --- |
| `clippy` | `clippy_gateway`, `clippy_data`, `clippy_rest` |
| `test` | `test_gateway`, `test_data`, `check_data_features`, `test_rest`, `test_data_adapters`, `check_integration_scenarios` |
| `data_db_smoke` | `data_db_smoke_postgres` |
| `check` | `frontend`, `fmt`, `clippy`, `test`, `data_db_smoke`, `shell_security` |

All 17 job definitions/IDs remain. Exact shell non-success comparisons and `always()` aggregate conditions remain; data feature matrix, PostgreSQL smoke and mandatory local PostgreSQL behavior are not reduced. No nightly/release workflow, concurrency, permission, billing, runner choice or release procedure changed.

## Actual verification

- PASS: `python tests/ci_contract_test.py`. Invokes the real dispatcher with mocked subprocesses, checking all exact argument arrays, inherited/overridden environment, repo cwd, parent environment immutability, ordered preflight, dry-run nonexecution and equivalent plans, nonzero status propagation without retry, shared workflow invocation, single Gateway plan, gate graph, Rust version, mold scope, triggers and Makefile fronts.
- PASS: `python tools/ci.py preflight --dry-run`. Printed exactly three real plans; no Cargo command executed. This is not a Rust compilation/test PASS.
- PASS: installed PyYAML `BaseLoader` parsed the whole workflow. Compared current and `git show HEAD:.github/workflows/rust-ci.yml`: identical 17 job IDs, and every non-target job exactly equals baseline after disregarding only the newly explicit Rust setup version.
- Initial one-off parser check mistakenly expected 18 jobs and failed its count assertion; actual parsed baseline/current both contain 17. Replaced that ungrounded count with baseline equality and reran successfully; this was validation-command error, not malformed YAML.
- PASS: `D:/Program Files/Git/bin/bash.exe tests/release_supply_chain_test.sh` (read-only pins/provenance fixture).
- PASS: scoped `git diff --check -- .github/workflows/rust-ci.yml Makefile apps/aether-gateway/AGENTS.md`; only pre-existing Git LF/CRLF conversion notices appeared.
- Build-watch fixture belongs to the parallel build writer; this leaf references its agreed final path but did not run it while the writer was active. Root/check owns combined verification after handoff.

## Boundaries and follow-ups

- No actual preflight/Gateway compilation, frontend test, real database, new dependency installation, remote workflow operation, push, release, deployment or user environment change occurred.
- No temporary files, generated bytecode caches or persistent task-owned process were created. Other writer's build.rs/build-watch changes were preserved.
- Expected structural benefits: one local/CI command source, one Gateway Cargo/nextest planning boundary, complete sibling failure feedback, correct cache visibility of mold flags and less unused toolchain setup. No runtime savings percentage or actual GitHub speed improvement is claimed.
- Retained limitations: distinct lib/bin artifacts still need compilation; no-fail-fast failure runs may take longer per run but expose more failures in that run; full OS/linker/database environment parity and actual remote timing remain unmeasured.
