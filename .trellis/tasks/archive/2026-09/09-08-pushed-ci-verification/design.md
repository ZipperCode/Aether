# 精确 SHA CI 实测与修复

- 已完成首推：`948c1c16f` → Rust CI `34210248162`，先按现有授权push，再处理证据，不加前置全量门槛。
- 第一波在不可变远端SHA运行期间，可以修本地已经确证的互不重叠失败；未收集完本轮失败前不急于push取消正在运行的job。
- 每个写代理独占明确文件；日志收集保持只读或仅写当前任务research，Root拥有工件、提交/push、最终精确SHA核验。
- CLI不提供运行中工作流日志时，可只读使用GitHub job logs API读取已完成job；不得绕过权限、读取凭据或输出敏感值。
- CI颜色输出/宿主配置属于被解析工具的显式输入，测试必须在自身边界保持确定性，不通过删除检查忽略问题。
- 失败修复按现有业务与平台合同，不扩展兼容层/保护/依赖/设置；共享源或公共接口由唯一integrator串行收口。
- 最終跑过的CI jobs和结论属于指定headSha；代码变化后不复用旧SHA的整体CI成功声明。
