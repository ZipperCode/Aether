# Second Workspace Rest repair check

- Status: READY; reviewer has stopped writes and returned the four assigned Rust files and Cargo execution slot to Root. Root accepted the explicit Windows-only lint and Tunnel runtime limits below; this is not an overall lint/CI PASS.
- Baseline: `9026380d1957e7a57a746a409335f15eb1675515`; actual completed run `34217818968` exposed five Workspace Rest failures. The same run passed all 5,456 Gateway tests; it is not an overall CI PASS.
- Context: complete saved check hook, current PRD/design/implement, four second-CI repair receipts, completed-run evidence, package quality indexes/guidelines, Tunnel AGENTS, and Trellis check/ponytail instructions were read. CodeGraph was consulted first, then exact source/caller reads filled its omitted ranges.

## Findings (fixed)

No additional source defect found in the assigned change set; the reviewer has not changed a product file. Existing fixes were verified against the actual paths:

- `crates/aether-model-fetch/src/strategy.rs`: the fixture still fails page two with HTTP 503. `execution_result_error_message` and the existing finite diagnostic projection produce the exact fixed service-failure text; the Vertex aggregate preserves the failed Endpoint exclusion. The updated equality is stricter than the obsolete raw-message substring assertion. Partial-page discard, other-source positive evidence, authority exclusion, and request-count assertions remain intact. Production unchanged.
- `crates/aether-oauth/src/provider/providers/kiro.rs`: missing Profile intentionally follows social refresh with Profile discovery. The reused `RoutingExecutor` records both real requests instead of overwriting the first. Exact safe URLs, request order/count, methods, token body, discovery Host/Bearer and returned Profile are asserted; malicious-region cases and production normalization remain unchanged. Both shared-mock consumers were read. Production unchanged.
- `apps/aether-tunnel/src/tunnel/stream_handler.rs`: both real collector rejection branches already send the fixed `response_too_large` category. The single added exact match repairs its loss in the shared sending-side projection. `StreamError` remains plain UTF-8, distinct from JSON `ResetStream`; limits, body forwarding, metrics and the two original assertions are unchanged. Unknown diagnostic text still follows existing categories. This is the batch's only production change.
- `crates/aether-usage/runtime/src/runtime.rs`: the release `Notified` is created before announcing policy entry, repairing the shared test fixture's observed lost-broadcast interleaving. The deterministic standard-library Wake witness exercises that exact window. Original admission/order/counter tests and production scheduler/capture behavior are unchanged; all three fixture callers were reviewed.

## Verification

All Cargo processes use pinned Rust `1.95.0`, host `x86_64-pc-windows-msvc`, the existing main target directory, and process-local `CARGO_INCREMENTAL=0`, dev/test debug `0`, jobs `2`, `RUST_MIN_STACK=16777216`. Existing NASM and Visual Studio CMake paths were verified on disk and added only to child-process environment; no `CMAKE_GENERATOR`, global environment or toolchain/dependency installation.

- PASS: `rustfmt --edition 2021 --check --config skip_children=true` on all four assigned files; scoped `git diff --check`.
- PASS: `cargo test -p aether-model-fetch --lib strategy::tests::vertex_pagination_failure_does_not_claim_endpoint_authority --locked -- --exact --nocapture`; session `57774`, exit 0, build 2m38s, 1/1 test in 0.00s.
- PASS: same fresh model-fetch executable, exact names first confirmed with `--list`, then `strategy::tests::vertex_partial_failure_on_one_endpoint_keeps_positive_evidence_without_authority` and `strategy::tests::execution_result_error_projection_discards_execution_error_message` each ran 1/1 in 0.00s. Total model-fetch: 3 distinct PASS. Artifact `target/debug/deps/aether_model_fetch-1ca55c8d2e115552.exe`, SHA-256 `459B0D0C7489961D495DB18D00E44BB8710FED687305B52899EAF233D5E02615`.
- Valid usage owner results reused: four distinct targeted tests PASS, including the deterministic witness (previously reproduced RED), original failure, backlog and admission-recovery siblings. Final current artifact hash was independently rechecked: `target/debug/deps/aether_usage_runtime-41bec7712d3918ad.exe`, SHA-256 `C5374CB4D29150CDE6248EFC7BAA9EFB6263F9F9E4577759531F75BC83731896`. No related source has changed since that final build. Full commands/timings are in `second-ci-usage-order.md`; no duplicate usage run was started.
- PASS: `cargo test -p aether-oauth --lib provider::providers::kiro::tests::refresh_urls_use_safe_default_for_malicious_auth_region --locked -- --exact --nocapture`; session `21489`, exit 0, build 26.87s, 1/1 test in 0.00s. Same fresh binary, exact name confirmed with `--list`, then `provider::providers::kiro::tests::refresh_discovers_missing_idc_profile_arn` ran 1/1 in 0.00s. Total OAuth: 2 distinct PASS. Artifact `target/debug/deps/aether_oauth-35a0ea545bb673a0.exe`, SHA-256 `28A08CBAB92B27147EE85332B83AA4FBBE160173EB2671AB554774E32EAB71ED`.
- FAIL (preexisting Windows-only lint, accepted by Root): `cargo clippy -p aether-tunnel --bin aether-tunnel --no-deps --locked -- -D warnings`; session `39136`, exit 1. The only diagnostics are `dead_code` for `apps/aether-tunnel/src/setup/service.rs:798` (`validate_private_service_directory`), `:844` (`validate_replaceable_service_file`), and `apps/aether-tunnel/src/setup/upgrade.rs:754` (`sync_upgrade_directory`). `git diff --quiet HEAD` verified both files unchanged; platform-gated source was inspected. Root explicitly declined unrelated edits or lint allowances. No Gateway service target was compiled; the distinct `aether-gateway-tunnel` shared-contract crate was checked as a normal dependency.
- TypeCheck: PASS for the model-fetch/OAuth current test targets and the valid reused usage build. Tunnel strict Clippy did not pass, so no complete Tunnel native type/lint PASS is claimed. Final four-file rustfmt rerun exited 0. Runtime total: 9 distinct targeted tests PASS (5 freshly run, 4 valid reused); Tunnel's 2 original tests NOT RUN locally.

## Explicit limits and Root decisions

- The Tunnel manifest has a direct `aether-gateway` testkit dev-dependency. The reviewer identified this before invoking Tunnel tests, avoiding the forbidden Gateway rebuild. Root explicitly selected production-only `cargo clippy -p aether-tunnel --bin aether-tunnel --no-deps --locked -- -D warnings`, source-path review, and the next exact-SHA Linux CI for the two original Tunnel behavior tests. Those two tests are NOT local PASS; no manifest edit or substitute harness is authorized.
- Root accepted the three preexisting Windows `dead_code` findings as a nonblocking local verification limit. They are not repaired here; Linux Clippy and the two original Tunnel behavior regressions remain the next exact-SHA CI gates. No warnings were suppressed and no failing check was relabeled PASS.
- Gateway's existing `safe_peer_stream_error` separately projects received text to generic relay categories. Root explicitly declined expanding that unrelated receiving-side behavior. No end-to-end client-visible `response_too_large` preservation is claimed.
- Root-owned spec follow-up: retain known fixed StreamError categories in the sending-side projection; blocking cross-runtime fixtures using `notify_waiters` must establish the release future before publishing their start signal. No new OAuth/model-fetch product contract was introduced.
- No full package/workspace/Gateway suite, provider/OAuth network operation, database, CI operation, Git mutation, release or deployment. Exact next-SHA full Linux success remains Root's gate.

## Cleanup

- Only this retained research receipt has been written by the reviewer. Existing product/other-agent changes are preserved.
- All three Cargo sessions and direct test processes exited. Final process inventory contains no Cargo/rustc/clippy-driver process; no persistent task-owned process remains. Standard ignored Cargo artifacts remain for reuse; no temporary source, debug helper, alternative harness, extra dependency, generated-source edit, external operation or cleanup deletion was performed.
- Final source hashes match the review snapshot: Tunnel `2DEFA530BA3237F65BF165C091AA24810D459CFB0C49330C560136A8749660BE`; model-fetch `BCD93989E49D66897C98EA326A6E53834A2E6A09C98DF75FDF870D74F65081B5`; OAuth `7C107C8133E07D522DDA1A118CE32CD5D0A35613138205DF515AD1C17C7CA0B1`; usage `A0E264DC87AC0AC4901249365687063657C4D141F0234FC23417BE3FEAA08B28`. No product change during review invalidated the runtime evidence.
