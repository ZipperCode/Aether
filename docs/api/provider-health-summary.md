# 提供商管理的端点健康度

以下管理接口使用相同的健康度汇总规则：

- `GET /api/admin/providers/summary`
- `GET /api/admin/providers/{provider_id}/summary`

## 统计规则

- 仅统计启用端点下、支持该端点 API 格式的启用密钥。
- 每个密钥读取其 `health_by_format[api_format].health_score`，不跨格式借用分数。
- 有有效健康分数时只对真实分数求算术平均，不把未观测密钥按满分加入平均值。
- 启用端点没有关联密钥，或至少有一个启用密钥但尚无有效分数时，摘要返回初始健康度 `1.0`；此值不代表主动探测成功。
- 分数范围为 `0` 到 `1`，沿用调度器的分数读取与范围约束。
- 停用端点或只有停用密钥时，端点的 `health_score` 返回 `null`。
- `avg_health_score` 平均所有非 `null` 的端点分数（含初始满分）；没有此类端点时返回 `null`。
- `unhealthy_endpoints` 仅统计上述端点中健康度低于 `0.5` 的数量，不把未知状态算作故障。

`total_keys` 和 `active_keys` 仍反映密钥配置数量，不因缺少健康数据而减少。

## 数据读取

摘要从密钥的轻量投影读取 API 格式、启用状态和 `health_by_format`。
PostgreSQL 投影中的凭据字段使用 `summary` / `{}` 等脱敏占位值，并非真实密文，
因此摘要读取不执行凭据解密、认证或迁移。完整密钥读取仍保留原有的凭据安全校验。

若将这些占位值送入凭据校验，读取会失败，并被摘要聚合当作空密钥列表；
读取或投影失败必须在数据读取层修复，不能用健康度默认值掩盖配置读取错误。

## 页面展示

桌面表格和手机卡片使用相同规则：

- 有健康数据时显示百分比；有效的零分显示 `0%`。
- 已配置启用密钥且尚未产生观测时显示绿色满格 `100%`，作为初始健康度。
- 端点停用、未配置密钥或只有停用密钥时显示灰色占位条和对应状态提示；未配置密钥时，即使摘要返回初始满分，页面仍显示灰色。

此分数是密钥当前健康状态的聚合，不是某个时间窗口内的请求成功率，也不是主动探测结果。

## 边界与验证

| 场景 | 摘要健康分 | 页面显示 |
| --- | --- | --- |
| 启用端点及密钥，尚无观测 | `1.0` | 绿色 `100%` |
| 启用端点，无关联密钥 | `1.0` | 灰色 `-`，未配置密钥 |
| 同端点一个密钥为 `0.2`，另一个尚无观测 | `0.2` | `20%` |
| 端点停用，或只有停用密钥 | `null` | 灰色 `-` |
| 没有端点 | `avg_health_score: null` | 无端点健康条 |

正确做法是只在整个启用端点没有有效观测时应用初始分；错误做法是给每个缺失分数填 `1.0` 后求平均，这会抬高已有故障记录的端点健康度。

回归检查：`cargo test --locked -p aether-gateway --lib admin_provider_summary_health -- --nocapture`。
测试覆盖初始满分、停用过滤、真实零分、未观测密钥不抬高同端点分数、跨端点平均及脱敏摘要投影；对应断言位于 `apps/aether-gateway/src/tests/control/admin/providers.rs`。
